# Worklog

## 2026-09-09 · Порядок в git + читаемый отчёт review-скиллов (issue #12)

- **Git:** локальный `main` отставал от `origin/main` на 5 коммитов и держал июльские доки; незакоммиченный diff и старый handoff вынесены в drafts, `main` fast-forward до `origin/main`, затем до `feat/review-skills` (PR #11 влит, push OK). Мёртвый тред «AIPM v1» (rate limit + протухший OAuth) заархивирован. Новая ветка `feat/readable-report`.
- **Диагноз:** оба демо-прогона `aipm-review-signal-problem` 04.09 упали на preflight: переход D1/D2/D5 не был передан в промпте. Вывод скиллов подтверждён нечитаемым: коды в первой строке, YAML на 60 строк, у solution ID критериев и 35–46 строк без машинного слоя.
- **BMAD:** `bmad-help` → `bmad-spec` (планировочных артефактов нет, задача узкая). Спека `docs/specs/spec-readable-report/`: 6 CAP, машинный слой не меняется (потребителей нет), человеческий слой — детерминированный перевод по закрытому глоссарию, единый макет для обоих скиллов, `BLOCKED` рендерится одним вопросом. Самопроверка PASS/PASS.
- **Реализация (plugin 0.3.0):** переписаны оба `report-template.md`, секции Output format / DoD / Failure handling в SKILL.md, добавлен `assets/glossary.md` (код → фраза). Проверка `check_human_layer.py`: старые демо-отчёты FAIL (как и ожидалось), самотест OK.

- **Смоук (codex-тред):** golden-документ «Условия сотрудничества», D5 → «Недостаточно: 6 препятствий», человеческий слой 18 строк без кодов, машинный слой под `<details>` с полной схемой, `check_human_layer.py: OK`. Отчёт лежит вне репо (real data).

**Next:** калибровка на 4 документах владельца → 5 прогонов в чистых тредах → финальный формат в шаблоне.

## 2026-09-10 · Калибровка на 6 документах + починка доступа к Confluence

- **Доступ:** MCP Hub в сессии Claude падал по таймауту, codex-треды не видели Confluence. Причина: у локального прокси (LaunchAgent `com.mcp.mcp-hub`, supergateway → `avito mcp hub`) протух OAuth-токен, дочерний процесс умирал. Лечение: `avito-ai mcp-hub auth` (браузер, секунды). Проверено: codex-тред читает страницу через `mcp-hub → call_tool → confluence-mcp`. Обход на будущее: `avito-ai mcp hub call` из терминала.
- **Кандидаты:** 16 discovery-документов по Travel (STR), Недвижимости (REAL) и Gig (JAZZ, SE) плюс 2 от владельца и 2 от соавтора. Пустые шаблоны REAL отброшены.
- **Калибровка 6/6** (A D3, B D4, C D2, D D5, E D4, F D2; codex-треды на локальных копиях): `check_human_layer.py` OK у всех, человеческий слой 17–23 строки. Утечки формата: solution выдавал «Вердикт» раньше заголовка (старое «вердикт первой строкой» в SKILL.md), «дописать недостающие атомы» и `assets/document-skeleton.md` из карты причин, «шапка воздействий» в границах, «В документе: проверить нельзя», служебный префикс в заголовке пункта. Всё исправлено; в проверку добавлен список запрещённого жаргона.
- **Находка по рубрике, не по формату:** C (исследование смешанных выдач, D2) заблокирован preflight-правилом DB-09 из-за пункта «продолжить доработки грамматики (tech)» в «Дальнейших планах». Решение владельцу: считать ли раздел планов заявленным commitment.

**Next:** 5 прогонов в чистых тредах на исправленных шаблонах → финальный формат.

## 2026-09-10 · Финал: 5 прогонов в чистых codex-тредах

- **5/5** (G D3 скидки BBiP, H D5 добиллинг CPA, I D2 дома Travel, J D1 ТМС-гео, K D4 бейдж Подработки): `check_human_layer.py` OK, 21–23 строки, ни одного кода и служебного слова. Треды сами называли, что ещё непонятно без словаря: только доменные термины из самих документов (ТМС-партнёры, «красные» селлеры, net-выручка, «потолок»).
- **Дрейф и правки:** три отчёта из пяти потеряли название документа в заголовке → правило в обоих шаблонах и проверка в скрипте. I заблокирован по недоступным источникам, но писал «Проверка не началась» и перечислял препятствия → в глоссарии BLOCKED разведён на «по входу» (один вопрос) и «по источникам» («Проверить нельзя без …», блоки остаются).
- **Наблюдение по методике:** прогоны идут на локальных экспортах с закрытыми ссылками, поэтому документы, чьи доказательства живут в дашбордах, упираются в «источники не переданы». Это не дефект формата; для боевого использования скилл читает Confluence и дочерние страницы сам.
- **Итог по issue #12:** success signal спеки достигнут на 11 реальных документах. PR #13 готов к мержу. Открыт один вопрос по рубрике: правило DB-09 (C, «Дальнейшие планы» как commitment).

## 2026-09-10 · Тред на документ: отчёт + разбор, DB-09 по заявленному шагу

- **Решения владельца:** DB-09 смягчён (commitment — только явно заявленный следующий шаг; пункты «Дальнейших планов» не блокируют); новый режим «разбор» — второе сообщение в треде документа: рендер машинного слоя по разделам документа с ✅/❌/➖, цитатой и причиной, в конце «Итого» и «что показать, чтобы ❌ стала ✅». Формат работы: тред на документ, документ приложен, сообщение 1 — отчёт, сообщение 2 — разбор.
- **Проверка смягчения:** C («Смешанные выдачи в Travel», D2) больше не блокируется на входе; теперь «проверить нельзя без графиков» по недоступным источникам, 86 проверок в разборе, 32 пройдено.
- **11 тредов (A–K):** отчёты 11/11, разборы 10/11 на момент записи. Шум в разборах solution-скилла: ➖ по причине «нет блока воздействий» повторялся 10–12 строками (B, G, K) → правило сворачивания в одну строку добавлено в оба шаблона.
- **Индекс для вычитки:** `~/Vibe/work/avito/raw/drafts/ai-for-pm/runs/INDEX.md` (вне репо): документ → тред → вердикт → отчёт → разбор → итог.
- **Демо-инструкция** выдана владельцу: тред A как главный пример, живой запуск на D (D5, 4–6 минут), промпты для обоих скиллов.

**Next:** вычитка владельцем 11 разборов → правки формата/рубрики по замечаниям → PR в main.

## 2026-09-10 · AVT-13: local project relocation

- Moved the complete repository under Growth, retaining the project name, Git origin, existing branches and worktrees, and BB project/source IDs.
- Preserved both old-path aliases for existing sessions and references. Kept local attachments excluded from Git; nothing was published.
- Verified the full local backup, original file hashes, Git refs and indices, worktree access through both paths, and the unchanged owner-branch worklog.
- Prepared navigation and canonical context on a dedicated migration branch. Existing worktree HEADs and plugin content were not changed; SessionStart hooks were not activated.

**Next:** review and adopt the migration context without replacing existing worktree context, then perform the separately authorized startup check.

## 2026-09-10 · AVT-13: branch-safe startup registration

- Authorized additive context-only commits for main and both existing feature worktrees; all preceding commits and plugin/spec bytes are retained.
- Registered the existing shared SessionStart validator in each checkout with its own local canonical files and root alias. No loader or plugin implementation changed.

**Next:** verify static old/new-path startup checks, then the single authorized fresh Codex main startup.

## 2026-09-10 · Перенос находок Codex-ревью PR #11 в main (плагин 0.4.1)

- **Источник:** ветка `fix/pr11-review-fixes` и черновой PR #14 другой сессии закрывали три замечания автоматического ревьюера на PR #11 (P1 ER-02, P1 ложная готовность D4, P2 согласие на запись), но были сделаны от версии до читаемого отчёта и конфликтовали с ним в трёх файлах.
- **Решение:** не сливать ветку, а перенести смысл руками поверх текущих шаблонов: строка ER-02 в каталоге signal-problem; D4 при интервале «стоп/заморозить/сменить вариант» даёт «не достаточно для D4» с маршрутом к D5 или повторному D3 (каталог D4 п. 2, SKILL.md шаг 9, подсказка в «Почему» шаблона); запись в документ только по явной просьбе, доступ на запись согласием не является (SKILL.md шаг 10 и шаблон, где та же дыра была и в новом тексте). Английские вставки переведены. `docs/reviews/pr11-follow-up.md` перенесён с пометкой о переносе.
- **Риски и снятие:** конфликт с чужим worktree — не тронут; расхождение вердиктов по уже прогнанным D4-документам — у B, E, K результат первой проверки не записан, п. 1 и так не пройден, вердикт не меняется; регрессия формата — контрольный прогон solution-скилла на E после мержа.
- **PR #14** закрыт как заменённый, ветка `fix/pr11-review-fixes` оставлена владельцу той сессии.


## 2026-09-10 · Контроль формата solution 0.4.1

- Источник: BB thread `thr_39k4a93rxp` (контрольный прогон по явному запросу пользователя).
- Заново прочитаны скилл и все четыре assets; выполнен локальный прогон с ограниченным составом источников и независимой проверкой выводов. Отчёт сохранён только в ignored local-research; его содержание не публикуется.
- Проверки: `check_human_layer.py` — `OK`, код выхода 0; SHA-256 исходного файла до и после совпал. Ссылки и вложения не открывались. Код и версия плагина не изменялись.
- Следующий шаг: использовать локальный отчёт как результат контрольного прогона; новых изменений скилла этим прогоном не принято.
