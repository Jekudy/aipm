---
topic: Читаемый отчёт review-скиллов aipm (issue #12): человеческий слой на русском поверх неизменного машинного слоя
updated: 2026-09-10T19:18
---

- (direction by user) Вывод скиллов нечитаем: коды в первой строке и YAML на 60 строк; нужен structured output плюс перевод на человеческий язык; проверить на 4 документах пользователя, затем 5 прогонов в чистых тредах
- (decision) Машинный слой не меняем (схема YAML signal-problem и таблица «Результаты проверок» solution): потребителей у него сейчас нет (backlog-add и gsheet-export его не читают), значит унификация схем — YAGNI
- (decision) Человеческий слой — детерминированный рендер машинного слоя по закрытому глоссарию код→фраза; новых фактов в нём быть не может
- (decision) Единый макет для обоих скиллов: Что проверяли → Вердикт → Почему → Чего не хватает (до 3) → Что перевернуло бы вывод → Границы → машинный слой в свёрнутом блоке details в конце
- (decision) Коды переходов D1–D5 всегда сопровождаются расшифровкой словами при первом упоминании; ID критериев и enum-значения в человеческом слое запрещены
- (decision) BLOCKED рендерится как «Проверка не началась» плюс ровно один вопрос с вариантами словами
- (capability) CAP-1 первый экран читается без словаря
- (capability) CAP-2 блокер = что не так + цитата/место + что сделать + кто + когда готово
- (capability) CAP-3 машинный слой сохранён байт-в-байт по схеме, спрятан под details
- (capability) CAP-4 один макет для обоих скиллов
- (capability) CAP-5 блокировка читается как один вопрос
- (assumption) BB и GitHub рендерят HTML details/summary в markdown; при отказе рендера блок остаётся читаемым как обычный YAML в конце
- (question) Показывать ли счётчик пройденных проверок («пройдено 5 из 8»)? Сейчас правило «наружу только расхождения» и запрет процентов готовности; счётчик — пограничный случай
- (question) Нужен ли машинный слой ещё и отдельным файлом рядом с документом (для будущего экспорта в бэклог), или свёрнутого блока достаточно?
- (capability) CAP-6 человеческий слой — перевод машинного, не вторая оценка: каждый пункт соответствует ровно одному issue
- (event) Самопроверка, проход 1 (когерентность, правила 1–6, 8): PASS; CAP-3 success называет details как место размещения — это контракт, не реализация
- (decision) Проход 2 (сохранность): из solution-шаблона терялись строки «Расхождения», «Действие вне блокеров: шапка воздействий», «Стадия и дельта» — возвращены в макет как опциональные строки; «В очереди после блокеров» заменено фразой «ещё m в машинном слое»
- (event) Самопроверка, проход 2 (сохранность): PASS после правки макета. Wrapper-only content: нет
- (decision) Открытые вопросы решены по умолчанию до ответа владельца: счётчик пройденных проверок не показываем; отдельный файл машинного слоя не пишем
- (event) Спека готова; переход к реализации в шаблонах обоих скиллов
- (decision) ponytail-review: report-layout.md удалён как дубликат двух report-template.md; оба шаблона приняты как adopted companions; sources убраны
- (event) Смоук-прогон thr_gmcdrrryaf (partner-terms, D5): человеческий слой 18 строк, check OK, YAML под details — CAP-1/2/3/5/6 подтверждены на одном документе
- (event) Калибровка 6/6 (A D3, B D4, C D2, D D5, E D4, F D2): check OK у всех; найдено 5 утечек формата — порядок блоков в solution (SKILL.md «вердикт первой строкой»), «недостающие атомы» и путь assets/ из reason-action-map, «шапка воздействий» в границах, «проверить нельзя» в опровержении, префикс причины в заголовке пункта. Все исправлены; C заблокирован по DB-09 — вопрос рубрики, вынесен владельцу
- (event) Финал 5/5 (G D3, H D5, I D2, J D1, K D4) на исправленных шаблонах: check OK, 21–23 строки. Дрейф: 3 из 5 без названия документа в заголовке; I при BLOCKED_SOURCE писал «не началась», но перечислял препятствия. Правки: заголовок обязателен (правило + проверка), BLOCKED разведён на «по входу» и «по источникам». Success signal спеки достигнут
- (decision by user) DB-09 смягчён: commitment — только явно заявленный следующий шаг/решение; пункты планов не считаются; при сомнении issue LOWER_TRANSITION вместо блокировки
- (decision by user) Новый режим «разбор» (CAP-7): отдельным сообщением после отчёта рендер машинного слоя в порядке документа с ✅/❌/➖ и цитатами, показывается и пройденное; счётчики допустимы, проценты нет. Формат работы: тред на документ, сообщение 1 — отчёт, сообщение 2 — разбор
- (event) Фаза «тред на документ»: 11 тредов с приложенным документом, сообщение 1 — отчёт, сообщение 2 — разбор (bb thread queue create авто-отправляет после idle). Разборы 10/11 готовы; шум: ➖ по одной причине повторялся 10–12 раз (G, K, B) — правило сворачивания добавлено в шаблон. Индекс: runs/INDEX.md вне репо
- (decision) Перенос трёх P1/P2 находок Codex-ревью PR #11 поверх читаемого формата: ER-02 в каталоге, D4 не готов при стоп/заморозить/сменить вариант (маршрут D5/D3, подсказка в «Почему»), запись в документ только по явной просьбе. Ручной перенос вместо git-merge ветки fix/pr11-review-fixes; PR #14 закрыт как заменённый; плагин 0.4.1
- (decision by user) Проход-опровержение в solution-скилле удалён (0.4.2, cherry-pick aebbd26 из ветки треда A); в signal-problem строка «что перевернуло бы вывод» пока остаётся
