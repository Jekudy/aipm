<!-- Root: ~/Vibe/CLAUDE.md — ALWAYS read it first for vault-wide rules and structure -->

# Solution Doc — component SPEC (COARSE STUB)

> **Status: M0.** Deliberately a ~one-paragraph sketch (lazy fractal). Do NOT design further yet.

## One-line intent

Same container/diagnosis shape as Opp Doc but for a chosen Solution — pour the PM's existing solution work in, score Q per section, flag white-spots, end in intent + invariants (EARS-ubiquitous) + acceptance criteria (EARS/Given-When-Then) that Planning and downstream teams consume.


## When to detail

Detail on the wave immediately after Opp Doc hits M2+ and produces its first real ranked Solution options moving to the Solution Backlog — that promotion is the natural trigger, since Solution Doc's input only exists once Opp Doc emits it. Reuses the SAME diagnosis prompt shape (new frozen spec + rubric, no new logic). Section spine (research-grounded): intent -> context/scope -> goals/non-goals -> design + trade-offs -> alternatives -> invariants -> acceptance criteria. Stays a coarse M0 stub until then; no per-section rubric authored yet.

