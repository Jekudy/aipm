<!-- Root: ~/Vibe/CLAUDE.md — ALWAYS read it first for vault-wide rules and structure -->

# Solution Backlog — module SPEC (COARSE STUB)

> **Status: M0.** Deliberately a ~one-paragraph sketch (lazy fractal). Do NOT design further yet.

## One-line intent

Keep the Solution backlog as a store of ranked solution options, each carrying the three-slot contract (intent + invariants + acceptance criteria) promoted out of Opp Docs; feeds Solution Doc and quarter Planning.

## When to detail

Detail once the first real ranked options are promoted out of an Opp Doc (its natural trigger). Contract shape is already fixed in `docs/architecture.md#Interfaces` — this module is a store + re-rank convention over it, not a diagnosis workflow.
