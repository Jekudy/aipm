<!-- Root: ~/Vibe/CLAUDE.md — ALWAYS read it first for vault-wide rules and structure -->

# Planning — component SPEC (COARSE STUB)

> **Status: M0.** Deliberately a ~one-paragraph sketch (lazy fractal). Do NOT design further yet.

## One-line intent

Decompose from an established current state to a desired state (TRIZ IKR / ideal final result), then deliver in value-ordered vertical-slice waves by component (sequential + parallel), with a capacity-vs-impact sanity gate — consuming Context outputs and the Solution Backlog as inputs.


## When to detail

Detail LAST — only when Context collection and the Solution Backlog are both populated enough to feed real current-state resources and ranked wave contents (it consumes the other four, so can't be detailed before them). NOT diagnosis-shaped. Coarse ordered-checklist stub (IKR desired-state -> Nine-Windows current-state -> backward decomposition -> vertical-slice waves -> Now/Next/Later depth cap -> RICE capacity gate) with the doc-contract interface, nothing runtime-heavy, until active.

