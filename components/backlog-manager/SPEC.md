<!-- Root: ~/Vibe/CLAUDE.md — ALWAYS read it first for vault-wide rules and structure -->

# Backlog manager — component SPEC (COARSE STUB)

> **Status: M0.** Deliberately a ~one-paragraph sketch (lazy fractal). Do NOT design further yet.

## One-line intent

Manage and refresh the Opportunity backlog and the Solution backlog — keep them as stores of problems/outcomes and ranked options (never a committed roadmap), consuming the doc contracts the other components emit.


## When to detail

Detail once both Opp Doc and Solution Doc produce real docs that need somewhere to land and re-rank (roughly Opp Doc at M3+ with enough entries that a plain Markdown list stops scaling). NOT diagnosis-shaped — likely just disk-state convention (opp-backlog.md, solution-backlog.md) + manual-invocation-gated writes; may need no engine at all. Coarse M0 box until the backlog files become unwieldy.

